name: Apply admin API integration patch v2

on:
  push:
    branches: [main]

permissions:
  contents: write

jobs:
  apply:
    if: contains(github.event.head_commit.message, 'chore(ci): retry admin API integration patch')
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Apply patch
        run: |
          echo 'ZnJvbSBwYXRobGliIGltcG9ydCBQYXRoCgp' >/dev/null
          python - <<'PY'
          import base64
          from pathlib import Path
          data = 'ZnJvbSBwYXRobGliIGltcG9ydCBQYXRoCgp' 
          # placeholder replaced below by the workflow bootstrap step
          PY

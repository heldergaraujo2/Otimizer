const API_BASE = window.OTIMIZER_API_BASE || "http://localhost:8000";
const $ = (id) => document.getElementById(id);
let map; let markers = []; let routeLine; let currentRoute = []; let selectedIndex = -1; let visitedStops = new Set();
let accessToken = sessionStorage.getItem("otimizer_access_token") || ""; let manualLocations = {};
const escapeHtml = (value) => String(value ?? "").replace(/[&<>\"]/g, (char) => ({"&":"&amp;","<":"&lt;",">":"&gt;","\"":"&quot;"}[char]));
function setStatus(message,state=""){const status=$("status");status.textContent=message;status.className=`status${state?` status-${state}`:""}`}
function showError(message){$("error").textContent=message;$("error").hidden=false} function clearError(){$("error").textContent="";$("error").hidden=true}
function showAccountError(message){const error=$("account-error");error.classList.remove("info");error.textContent=message;error.hidden=false} function clearAccountError(){const error=$("account-error");error.classList.remove("info");error.textContent="";error.hidden=true}
function authHeaders(){return accessToken?{Authorization:`Bearer ${accessToken}`}:{}}
function friendlyApiError(response,detail){if(response.status===401)return "Sua sessão expirou ou é necessário entrar na conta.";if(response.status===403)return detail||"Sua licença não permite gerar esta rota.";if(response.status===413)return `Arquivo muito grande. ${detail||"Envie um arquivo menor."}`;if(response.status===422)return `Arquivo ou rota inválida. ${detail||"Verifique os dados e tente novamente."}`;if(response.status===502)return `Falha no serviço de roteamento. ${detail||"Tente novamente em instantes."}`;return detail||`Falha ao otimizar (HTTP ${response.status}).`}
async function readApiError(response){const contentType=response.headers.get("content-type")||"";if(contentType.includes("application/json")){try{const result=await response.json();if(typeof result.detail==="string")return result.detail;if(result.detail?.message)return result.detail.message;return ""}catch(_){return ""}}try{return(await response.text()).trim()}catch(_){return ""}}
function renderSignedOut(){$("account-signed-out").hidden=false;$("account-signed-in").hidden=true;$("license-status").textContent="";$("renew").hidden=true;$("renew").disabled=false;$("renew").dataset.licenseId="";if($("admin-link"))$("admin-link").hidden=true}
function renderSignedIn(email,role){$("account-signed-out").hidden=true;$("account-signed-in").hidden=false;$("account-email").textContent=email;if($("admin-link"))$("admin-link").hidden=role!=="ADMIN"}
async function loadAccount(){if(!accessToken)return renderSignedOut();try{const response=await fetch(`${API_BASE}/auth/me`,{headers:authHeaders()});if(!response.ok)throw Error(await readApiError(response));const account=await response.json();renderSignedIn(account.email,account.role);await loadLicense()}catch(_){accessToken="";sessionStorage.removeItem("otimizer_access_token");renderSignedOut()}}
async function loadLicense(){if(!accessToken)return;const response=await fetch(`${API_BASE}/licenses/me`,{headers:authHeaders()});if(response.status===401)return logoutLocal();if(!response.ok){$("license-status").textContent="Não foi possível consultar a licença.";$("renew").hidden=true;return}const payload=await response.json();const license=payload.license;if(!license||!payload.active){$("license-status").textContent="Licença inativa. Renove para continuar usando o Otimizer.";$("renew").hidden=true;$("renew").dataset.licenseId="";return}const expires=new Date(license.expires_at).toLocaleString("pt-BR");const priceCents=Number(license.price_cents);const price=Number.isFinite(priceCents)?(priceCents/100).toLocaleString("pt-BR",{style:"currency",currency:"BRL"}):"valor não informado";$("license-status").textContent=`Licença ativa até ${expires} · Renovação: ${price}`;$("renew").hidden=false;$("renew").dataset.licenseId=license.license_id||""}
function logoutLocal(){accessToken="";sessionStorage.removeItem("otimizer_access_token");renderSignedOut();setStatus("Sessão encerrada")}
$("login").addEventListener("click",async()=>{clearAccountError();const email=$("login-email").value.trim();const password=$("login-password").value;if(!email||!password)return showAccountError("Informe e-mail e senha.");$("login").disabled=true;setStatus("Entrando…","loading");try{const response=await fetch(`${API_BASE}/auth/login`,{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({email,password})});if(!response.ok)throw Error(await readApiError(response)||"E-mail ou senha inválidos.");const result=await response.json();if(!result.access_token||!result.account?.email)throw Error("Resposta de autenticação inválida.");accessToken=result.access_token;sessionStorage.setItem("otimizer_access_token",accessToken);renderSignedIn(result.account.email,result.account.role);await loadLicense();setStatus("Conta conectada","success")}catch(err){showAccountError(err.message||"Não foi possível entrar.");setStatus("Erro","error")}finally{$("login").disabled=false}});
$("logout").addEventListener("click",async()=>{try{if(accessToken)await fetch(`${API_BASE}/auth/logout`,{method:"POST",headers:authHeaders()})}finally{logoutLocal()}});
$("renew").addEventListener("click",async()=>{const licenseId=$("renew").dataset.licenseId;if(!licenseId)return;$("renew").disabled=true;clearAccountError();try{const response=await fetch(`${API_BASE}/payments/pix`,{method:"POST",headers:{...authHeaders(),"Content-Type":"application/json"},body:JSON.stringify({license_id:licenseId})});if(!response.ok)throw Error(await readApiError(response));const charge=await response.json();if(!charge.payment_id)throw Error("O serviço de pagamento retornou uma cobrança inválida.");showAccountError(`Pix gerado. Copie o código de pagamento e conclua a cobrança. ID: ${charge.payment_id}`);$("account-error").classList.add("info")}catch(err){showAccountError(err.message||"Não foi possível gerar o Pix.")}finally{$("renew").disabled=false}});
$("file-input").addEventListener("change",(event)=>{const file=event.target.files[0];manualLocations={};$("file-name").textContent=file?file.name:"Selecione um arquivo .xlsx";$("optimize").disabled=!file;clearError();setStatus(file?"Arquivo pronto":"Pronto")});
window.otimizerManualLocationSelected=(stopId,latitude,longitude)=>{manualLocations[stopId]={stop_id:stopId,latitude,longitude};setStatus("Localização confirmada · recalculando toda a rota…","loading");$("optimize").click()};
$("close-detail").addEventListener("click",()=>{$("stop-detail").hidden=true;selectedIndex=-1});$("next-stop").addEventListener("click",()=>{if(selectedIndex<0)return;markStopVisited(selectedIndex);if(selectedIndex<currentRoute.length-1)showStop(selectedIndex+1)});
$("optimize").addEventListener("click",async()=>{const file=$("file-input").files[0];if(!file||$("optimize").disabled)return;const form=new FormData();form.append("file",file);form.append("objective",$("objective").value);form.append("return_to_start",$("return-to-start").value);if(Object.keys(manualLocations).length)form.append("manual_locations",JSON.stringify(Object.values(manualLocations)));const endpoint=(latId,lonId,latName,lonName)=>{const lat=$(latId).value.trim(),lon=$(lonId).value.trim();if(!lat&&!lon)return;if(!lat||!lon)throw Error("Preencha latitude e longitude do mesmo ponto.");form.append(latName,lat);form.append(lonName,lon)};clearError();$("optimize").disabled=true;setStatus(Object.keys(manualLocations).length?"Reotimizando toda a rota…":"Otimizando…","loading");try{endpoint("origin-lat","origin-lon","origin_latitude","origin_longitude");endpoint("destination-lat","destination-lon","destination_latitude","destination_longitude");if($("destination-lat").value.trim()&&$("return-to-start").value==="true")throw Error("Destino e retorno ao início não podem ser usados juntos.");const response=await fetch(`${API_BASE}/optimize`,{method:"POST",headers:authHeaders(),body:form});if(!response.ok){const detail=await readApiError(response);if(response.status===401)logoutLocal();const error=Error(friendlyApiError(response,detail));error.status=response.status;throw error}const result=await response.json();if(!Array.isArray(result.route)||!result.summary)throw Error("A API retornou uma resposta de rota inválida.");currentRoute=result.route;visitedStops=new Set();$("result").hidden=false;const summary=result.summary;const routedDeliveries=Number(summary.routed_deliveries),routedStops=Number(summary.routed_stops),pending=Number(summary.pending),distanceMeters=Number(summary.distance_meters),durationSeconds=Number(summary.duration_seconds),routingComplete=summary.routing_complete===true;$("delivery-count").textContent=Number.isFinite(routedDeliveries)?routedDeliveries:"—";$("stop-count").textContent=Number.isFinite(routedStops)?routedStops:"—";$("distance").textContent=Number.isFinite(distanceMeters)?`${(distanceMeters/1000).toFixed(1)} km${routingComplete?"":" (parcial)"}`:"—";$("duration").textContent=Number.isFinite(durationSeconds)?`${Math.round(durationSeconds/60)} min${routingComplete?"":" (parcial)"}`:"—";$("pending").textContent=Number.isFinite(pending)?`${pending} pendência(s)`:"Pendências não informadas";$("coverage").textContent=summary.coverage_complete?`✓ Rota completa · ${Number.isFinite(routedDeliveries)?routedDeliveries:"—"} entrega(s) · ${Number.isFinite(routedStops)?routedStops:"—"} parada(s)`: `⚠ Rota incompleta · ${Number.isFinite(routedDeliveries)?routedDeliveries:"—"} entrega(s) · ${Number.isFinite(routedStops)?routedStops:"—"} parada(s)`;$("coverage").classList.toggle("pending",!summary.coverage_complete||(Number.isFinite(pending)&&pending>0)||!routingComplete);renderStops(currentRoute);await renderMap(currentRoute);$("stop-detail").hidden=true;selectedIndex=-1;setStatus(routingComplete?"Rota pronta":"Rota pronta com pendência(s)",routingComplete?"success":"")}catch(err){const message=err instanceof TypeError?"Não foi possível conectar à API. Verifique se o backend está em execução.":(err.message||"Não foi possível otimizar a rota.");showError(message);setStatus("Erro","error")}finally{$("optimize").disabled=!$("file-input").files[0]}});
loadAccount();

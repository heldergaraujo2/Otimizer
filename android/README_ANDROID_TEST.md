# Android test

The debug APK uses the Android WebView shell and connects to the FastAPI backend configured in the app.

For the first physical-device test, run the backend on the PC LAN interface and configure the app with the PC LAN IPv4 address, for example `http://192.168.1.145:8000`.
